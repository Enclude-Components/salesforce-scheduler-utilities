minimum_cumulusci_version: '4.10.0'
project:
    name: salesforce-scheduler-utilities
    package:
        name: salesforce-scheduler-utilities
        namespace: enc_sfsch
        api_version: '67.0'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
